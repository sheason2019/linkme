struct User {
  int UserId;

  string Username;
  string Password;

  optional string AvatarUrl;
}

service UserService {
  // 接受登录信息，返回jwt
  string Login(User user);
  // 同Login
  string Regist(User user);
}